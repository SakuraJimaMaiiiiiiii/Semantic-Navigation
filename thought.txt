1.在训练步中，若发生碰撞则返回原点？

当前碰撞后原地悬停并 AUTO.LAND，不是返航

2.NED、ENU、机体系是否转换正确；

基本完成 已实现 NED、FRD、相机光学系以及显示用 NEU 转换

3.现在飞行的时候是根据位置飞行 机头永远对一边

已解决 face_path=True 时机头跟随飞行方向

4.我想用速度 + 时间步进行控制

已完成 20 Hz 速度闭环，使用机体系 SendVelFRD

5.如何使用照相机：根据 VisionSensors_RGBD_Config_v0 和 sensors/rfly_rgbd_camera.py

已完成 已加载 JSON、同步 RGB/Depth、获取内外参

6.RGB目标检测 → 查询目标深度 → 结合相机和无人机位姿投影到世界坐标

部分完成  已有独立车辆检测，已接入实时 RGB-D 流
检测框查询深度  未完成  尚未从目标框提取深度
目标投影到世界坐标  几何基础完成  通用相机点云可以投影，但检测目标尚未接入

7.体素化与你后续的工作高度相关，它通常意味着：
输入：无人机获得的 RGB、深度、激光雷达以及位姿信息；
过程：利用位姿将每一帧观测投影到世界坐标系，并更新对应的体素（Voxel）；
体素存储的信息：
是否被占据（Occupancy）
语义类别（Car、SUV、Fire Extinguisher、Exit 等）
颜色/RGB
置信度
被观察次数
输出：形成一张三维语义体素地图（Semantic Voxel Map），供后续的目标搜索、路径规划和LLM任务推理使用。

因此，这里的“体素化”本质上就是把机器人沿途连续获得的观测信息，统一投影并离散到固定的三维体素网格中，
从而构建一张稳定、可持续更新的全局语义地图。这也是许多现代语义导航系统
（如 Voxblox、OctoMap、OpenScene、ConceptFusion 等）的核心地图表示方式。

局部点云	已完成	滑动局部三维占据栅格和障碍膨胀已实现
全局点云	已完成	全局稀疏占据地图、保存、PLY导出已实现

8.yolo world  进行感知实例
坤哥的语义分割 模型可以换为yolo world 更改为yolo world权重
完成

9.体素化（局部空间邻域）

未完成  当前只有点云，没有占据、类别、颜色等语义字段

10.由landmark 形成图 来查阅各个物体之间的关系

未完成没有对象节点、空间关系边和查询接口

11.LLM语义任务解析

未完成目前只存在技术路线设想

12.把A*规划的二维图画出来
完成




# 深度图 + 同步无人机位姿
→ 转换为世界点云
→ 射线融合到滑动占据栅格
→ 在锁内离散化状态并膨胀障碍
→ 复制原点、时间戳和数组
→ 得到 OccupancyGridSnapshot
→ 交给 A* 规划器

合离线地图和实时感知数据，动态更新图以支持实时语义导航。全局导航依赖预存路径或动态生成的场景图，局部路径规划则基于实时 LiDAR 映射和动态障碍物移除。

YOLOWorld

开放词汇对象跟踪：
使用 YOLOWorld 检测 RGB 图像中的 2D 边界框和分割掩码，并通过 ByteTrack 进行多目标跟踪，获取目标状态信息。
结合 LiDAR 数据进行目标定位，将目标状态表示为包含描述、3D 边界框和位置的节点信息。
对于每个跟踪目标，如果不在图中则创建新节点，否则更新其位置。

自身运动估计：
通过 LiDAR-惯性里程计系统获取自身运动状态，当运动步长超过阈值时，将位置和速度存储为自身节点，并在图中添加新边。
自身节点和边存储在 Embodied Graph 中，用于后续的全局规划任务。

ppt里的局部避障



对A*进行升级




体素化时更好保存为 semantic distribution
Voxel(12,23,5)

Car       0.72
SUV       0.18
Floor     0.07
Wall      0.03
这样多帧融合时好处理 且分割网络会输出概率 可以把置信度累积进对应的voxel里

SUV Voxel                SUV Voxel
SUV Voxel                SUV Voxel
SUV Voxel                SUV Voxel
    ↓                       ↓
3D clustering           3D clustering
    ↓                        ↓
SUV_01                     SUV_02

RGB-D
  ↓
语义分割
  ↓
每个像素得到类别
  ↓
利用Depth恢复该像素3D位置
  ↓
相机坐标 → 机体坐标 → 世界坐标
  ↓
将“3D点 + 类别”写入语义地图
  ↓
同类点聚合成物体实例
  ↓
建立语义目标地图
  ↓
LLM/任务解析得到目标类别
  ↓
查询目标世界坐标
  ↓
路径规划 + 避障


SAM2柱子掩膜
→ 与深度图对齐
→ 提取柱子对应的三维点
→ 相机坐标转换到世界坐标
→ 多帧聚类与融合
→ 与已有柱子实例匹配
→ 更新语义实例地图

{
    "instance_id": "column_007",
    "class_name": "column",
    "aliases": [
        "concrete column",
        "parking garage pillar",
        "support pillar",
    ],
    "position_world": [12.4, -6.8, -1.2],
    "bbox_3d": {
        "min": [11.9, -7.2, -2.8],
        "max": [12.9, -6.4, 0.0],
    },
    "confidence": 0.86,
    "observation_count": 18,
    "last_seen": 1788400000.0,
    "is_static": True,
    "is_obstacle": True,
}


体坐标系->极坐标系
卡尔曼滤波->非线性


对墙体的处理