# P0–P1 语义导航任务接口与验收

新增的“先按前沿信息增益探索、返航后由另一终端指定目标”流程见 [前沿探索与两终端导航](深度优先探索与两终端导航.md)。以下原有 `run_demo.py` 单阶段入口仍可使用。

## 指定目标

`config/semantic_target.json` 内已提供带注释的配置示例。读取接口支持独立成行的 `//` 注释；不支持行尾注释或块注释，其余内容遵循 JSON 语法。

默认 `config/semantic_target.json` 内容为 `{"target": null}`，继续执行原有航点任务。没有默认替你选定车辆或柱子。

把该文件改成以下任一种请求，然后按原有顺序启动 WebSocket、UE、`python run_demo.py`。也可指定另一个文件：

```powershell
python run_demo.py --semantic-target config/my_target.json
```

按类别：

```json
{"target": {"class_name": "fire cabinet"}}
```

按实例：

```json
{"target": {"instance_id": "vehicle_02"}}
```

按颜色与附近地标：

```json
{
  "target": {
    "class_name": "vehicle",
    "color": "red",
    "near_instance_id": "column_03",
    "near_distance": 3.0,
    "min_confidence": 0.25,
    "min_observations": 3,
    "max_age": 120.0
  }
}
```

`class_name`、`instance_id` 至少给一个，同时给出时取交集。筛选不匹配时不会忽略条件改飞别的目标。`vehicle` 表示 car/truck/bus/van。其他入图类别：`column`、`signboard`、`exit sign`、`fire cabinet`。出口标牌单独归类，普通指示牌不视为出口。颜色仅适用于车辆的多帧颜色投票，不确定颜色不匹配。

实例 ID 使用在线稳定的 `runtime_instance_id`，来自本次运行。车辆与视频 ID 一致；柱子的导出重编号、视频编号可能不同，不要用 JSON 的行号或导出 `instance_id` 代替。柱子后续合并可能让被合并的运行时 ID 失效，此时按找不到指定实例处理。每次运行仍从空地图开始，ID 不跨运行复用。

直接调用 Python 的接口：

```python
from application import DemoRuntime
from navigation import SemanticTargetRequest

runtime = DemoRuntime(semantic_target=SemanticTargetRequest(
    class_name="vehicle", color="red",
))
# 后续照常调用 runtime.start() / runtime.run_mission() / runtime.close()，
# 并使用 run_demo.py 中的异常悬停、降落处理。
```

请求在启动时读取，是不可变的任务输入。修改文件后下一次启动生效；本版不在飞行途中热切换任务，也不接自然语言。上层 UI/LLM 将来只需生成同样的请求对象。

## P0：语义目标闭环

1. 感知线程每完成一个同步帧，发布独立的语义地图快照；控制线程不访问正在修改的 mapper。
2. 根据类别、运行时 ID、颜色、距离关系、置信度、观测次数、观测年龄查询候选，按距离排序。接近过程中优先保持已选目标。
3. 在物体水平包围盒外生成观察点，默认距表面 1.2 m；保持起飞后的飞行高度。只接受具有已知自由路径的观察点，不把物体中心作为飞行终点。
4. 全局平面 A* 提供通道路径，已知安全航段直接跟踪；局部阻挡时才由 EGO 生成定高避障轨迹。执行层检查路径与机体高度带、自由空间，拒绝未观测区域和越界轨迹。
5. 到达后面向目标，必须在转向完成后得到 3 个不同时间戳的新观测，并确认位置和表面间距，才报告成功。历史地图记忆不能代替到达确认。
6. 无路、航段超时或复核失败会记录原因并冷却候选、重选或搜索。搜索耗尽后任务返回失败，沿现有任务流程返航/降落。碰撞、感知失效、地图失效等异常传给入口执行降落处理。

配置位于 `config/semantic_navigation_config.py`。默认任务预算 300 s、搜索移动最多 30 次、接近/复核失败最多 3 次、搜索半径 30 m。所有距离以米计，世界坐标为本次飞行的本地 NED。

## P1：搜索、全局地图与属性

指定语义目标时自动启用全局占据地图，关闭全局窗口及回环校正，并设置未知空间为障碍。关闭回环是因为现有回环会校正全局点云，却未同步校正语义实例与控制坐标；本版统一使用当前 NED 坐标。原有返航规划继续使用全局地图。

地图没有符合条件的目标时，先原地分方向观察，再在已知可达自由区内选择靠近未知区域的探索边界。搜索航段之间检查新目标，找到后转入接近。探索有范围、步数和时间预算，不保证所有车库布局都能搜索成功。地图证据不足或狭窄通道缺乏完整机体净空时会停止，不把未知区域当作通道。

消防箱、指示牌、出口标牌已进入三维实例地图。非柱子地标不使用柱子的 1.5 m 近邻合并规则。柱子继续执行 2.2 m 可观测高度和长宽比过滤。

本版空间关系为“距离某实例不超过指定距离”；完整关系图、自然语言任务解析仍属 P2。目标定位来自可见表面，承重柱与顶梁接触关系、跨运行重定位和多高度搜索不在本版内。

## 评估输出

每次语义任务保存：

- `evaluation/semantic_navigation.json`：任务条件、成功/失败原因、目标 ID、状态事件、执行时长、采样轨迹长度、搜索次数、碰撞记录、推理平均耗时。
- `maps/semantic_instances.json`：原有最终实例地图，增加车辆颜色字段。
- `maps/semantic_observations.jsonl`：开启 `save_semantic_map` 时逐帧保存在线实例记录、传感器时间戳和推理耗时，可分析身份变化。

任务结果衡量从语义任务开始到结束的阶段，不含起飞、返航和降落。路径长度由控制周期位置采样累计；推理耗时是算法处理耗时，不等于相机采集到执行控制的全链路延迟。

汇总多次运行：

```powershell
python tools/evaluate_semantic_navigation.py data/runs/run_A data/runs/run_B --output data/evaluation.json
```

需要定位误差、重复率、召回率、ID 切换时，提供同一 NED 坐标系中的真值文件：

```json
{
  "instances": [
    {"instance_id": "gt_car_01", "class_name": "car", "position_world": [5.0, 0.0, -1.0]},
    {"instance_id": "gt_column_01", "class_name": "column", "position_world": [8.0, 2.0, -1.5]}
  ]
}
```

```powershell
python tools/evaluate_semantic_navigation.py data/runs/run_A --ground-truth data/ground_truth.json --match-distance 2.0
```

采用同类最近真值、距离门限的关联定义：多个预测落到同一真值计重复；定位 RMSE 每个真值取距离最近的匹配；身份切换只比较实际重新观测的帧，重复歧义帧不强行选 ID。无真值时不输出虚构的定位精度；没有逐帧记录时 ID 切换数为 null。UE 类别分割图本身不包含物体身份和世界位置，不能直接替代上述真值。

## 验收场景

自动测试覆盖：接口校验、条件组合、稳定 ID、线程快照、结构柱过滤、出口分类、净空检查、已知区绕障、未知区拒绝、探索预算、搜索中发现目标、到达后新帧复核、旧记忆拒绝、感知/地图失效、碰撞传播、EGO 跟踪接口保护和评估指标。

仿真联调时至少分别验证：可见目标、遮挡目标、未见目标、相邻同类目标、指定目标不存在、路径被堵、感知停止。用上面的结果文件进行多次统计。自动化测试使用模拟载具，不代表已经通过 UE/PX4 实际飞行验收。
