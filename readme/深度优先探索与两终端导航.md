# FUEL 思路前沿探索、定高 EGO 避障与两终端导航

本流程先探索并返航落地，再接受目标任务。第一终端保持载具、地图和感知连接；第二终端只提交目标请求。语义结构使用不含绝对坐标的 JSON 保存；A*、EGO 必需的几何数据另存二进制缓存。

## 启动方式

先按原有方式启动 WebSocket（`npm start`）和 UE/RflySim，之后：

**控制终端：**

```powershell
python run_explore.py
```

本程序只在启动时发送一次场景请求。执行起飞、前沿信息增益探索、返航、降落，确认落地和原点位置后输出 `READY` 并等待任务。保持这个终端运行。

**目标终端：**

修改 `config/semantic_target.json`，支持其中的独立 `//` 注释。例如：

```json
{"target": {"instance_id": "vehicle_02"}}
```

实例 ID 从本次输出的 `exploration/semantic_graph.json` 的 `objects[].id` 选择。也支持类别、颜色和邻近关系筛选。然后运行：

```powershell
python navigate_target.py --open-map
```

也可以只为本次任务在命令行指定目标（覆盖 target 文件，不修改文件）：

```powershell
python navigate_target.py --class-name vehicle --open-map
python navigate_target.py --instance-id vehicle_01 --open-map
```

`vehicle_01` 必须替换为本次语义图中的实际 ID。默认 `target: null` 表示未指定目标，并非连接或导航故障；程序不会自动选择物体起飞。

第二终端读取 target 文件或命令行目标并向当前会话提交请求。第一终端从保存的导航缓存查询对象，检查当前感知、地图、坐标连续性和可达性后，重新起飞。全局路径的已知安全航段直接跟踪；局部通道受阻时才启用 EGO 生成避障样条，前往安全观察点。到达后需要新视觉观测确认目标，然后默认返航、落地，重新进入 `READY`。后续修改 target 后再次执行该命令即可。

查询状态、打开地图和退出：

```powershell
python navigate_target.py --status --open-map
python navigate_target.py --shutdown
```

默认发现文件为 `data/exploration_session.json`。也可以显式指定本次会话文件：

```powershell
python navigate_target.py --session data/runs/run_.../exploration/session.json --target config/semantic_target.json
```

目标终端不启动 UE、不重置场景、不重新连接载具。不要同时运行另一个 `run_demo.py` 控制同一架无人机。默认控制端口为本机 `127.0.0.1:8877`，仅一个请求可以执行；探索、飞行期间的新任务会被拒绝。

## 前沿探索的实现范围

探索图的节点是已到达的前沿观察点，节点间的连边表示实际执行过的探索连接。

控制入口使用 `navigation.frontier_explorer.FrontierExplorer`，报告策略标识为 `fuel_inspired_frontier`，局部执行器标识为 `ego_only`。旧的 `DFSExplorer` 仅作为导入兼容别名，不执行深度优先遍历。

1. 起飞后只做一次四方向扫描，建立初始占据地图和语义观测。
2. 从已知自由空间与未知区域的交界提取连通前沿区域；较长的区域沿主方向拆分，默认最大跨度约 6 米。访问一次不会永久屏蔽整块空间，前沿随地图观测更新。
3. 每个区域采样多个安全观察位置和 8 个朝向，按相机水平视场和遮挡射线估算未知区域收益。已知障碍后方不计入可见收益。结合通道距离、转向代价，为最多 5 个候选区域计算短访问路线，再选择下一观察点。
4. 全局 A* 提供已知自由空间内的通道路径。路径航段安全且前向深度充足时直接跟踪，只有局部通道受阻时才调用 EGO 生成避障样条。中间引导点采用连续通过，途中持续建图和识别。
5. 到达观察点后面向选定的未知区域，短暂停留采集语义观测。没有可选前沿时，只朝预计可见未知区域最多的方向转向一次；没有信息收益时不旋转。初始建图仍保留一次四方向扫描。
6. 候选暂时为空时不会立即结束：最多朝两个不同的高收益方向分别观察一次，并要求“无可达前沿”连续确认至少 3 次且持续至少 10 秒。前沿确实为空时正常完成；若剩余前沿面积不超过 1 平方米，并且最近 5 个观察点新增地图面积不超过 1 平方米，也可按 `diminishing_information_gain` 正常完成。
7. 剩余候选被阻塞、搜索达到节点上限或超出探索半径时只标记为部分完成。路径失效时从当前位置重规划，不为维持 DFS 树而强制原路回退。

移动时由速度引导连续转向已检查的路径，不在每个重规划航段前停下并精确对准。航向偏差过大时飞控会暂时保持位置并转向，进入安全角度后继续前进。移动期间每 8 秒至少应产生 0.15 米水平位移，否则中止本次移动并记录 `FRONTIER_MOTION_BLOCKED`。失败观察位置/朝向冷却 30 秒后可重试，最多尝试 3 次；期间可选择该区域的其他位置或朝向。候选允许距当前位置 0.8 米以上，不因为视野内没有 3 米外的目标就结束。

执行层会记录 `LOCAL_EXECUTION` 状态。`route_clear` 表示沿全局前沿路径直接飞行、EGO 待命；`ego_avoid` 表示检测到局部阻挡后由 EGO 绕障。EGO 不可执行时悬停、换观察点；其内部用于生成优化种子的 A* 搜索仍属于 EGO 实现，不会直接输出栅格路径控制飞行。急停、过期地图等停止状态保留。路径失效或探索结束会先悬停，避免连续通过航点的剩余速度带入返航阶段。

探索、目标接近及正常返航以起飞后高度为参考（起飞高度默认 1.5 米，位于 `config/mission_config.py`）。EGO 的搜索限制在水平面内，Python 优化固定高度维度；C++ 优化结果在固定高度后重新验证碰撞和时间尺度。需要升降才能通过的路径不会执行，起飞/降落阶段除外。飞控会有小幅跟踪误差，因此不是物理高度绝对不变。

EGO 前视点沿已验证样条选择，避免每次重规划都落在起点附近、被飞控当成已经到达。时间推进的跟踪点仍经过执行层自由空间检查。此实现参考 FUEL 的前沿管理和分层访问思路，是本项目适配实现，并非移植或完整复现 FUEL。

语义结果超过 5 秒未更新时，探索先悬停等待，最多等待 `config/exploration_config.py` 中的 `perception_recovery_timeout`（默认 20 秒）。新帧到达后继续探索，悬停等待不计入无位移判定；等待期间仍检查地图更新和感知线程异常。日志记录 `PERCEPTION_WAIT`（帧龄和上次推理耗时）、`PERCEPTION_RECOVERED` 或 `PERCEPTION_RECOVERY_TIMEOUT`。持续超时仍中止任务，不使用过期语义结果继续采集。

`exploration_report.json` 及中间语义检查点包含执行状态和受阻原因。`frontiers_blocked` 表示仍有失败候选，`unobserved_frontiers` 表示仍有前沿但没有新的有效观察点，两者都不代表全图完成；手动中断记录为 `interrupted`。访问顺序使用不含坐标的前沿 ID。JSON 保存兼容 NumPy 数值，同时拒绝 NaN/Infinity，保存失败不会覆盖已有的有效文件。

导航使用深度观测自由空间及当前载具位姿提供的机身体积证据，并检查机体净空。相机近场盲区可能使无人机自身所在体素一直未观测；语义/探索模式会在每个有效建图帧中，补足当前机身覆盖的未观测体素（水平半径 0.35 米、上下半高 0.25 米，按栅格离散）。已有深度命中证据不会被该处理清除，机身以外的未知区域仍不可通行。此设置需与实际载具尺寸和位姿可靠性匹配。通道证据不足时不会把未知空间直接当作可飞通道。局部避障由 EGO 执行，探索保持起飞后的高度。

预算位于 `config/exploration_config.py`：默认任务时间 3600 秒，其中预留 120 秒返航；最多 600 个观察点、水平半径 100 米。`viewpoint_spacing` 是区域拆分尺度（默认 3 米，最大区域跨度约为两倍），信息收益半径 3 米；`camera_fov_degrees` 默认 90°，应与实际相机匹配；`tour_frontiers` 默认 5。当前位置自由空间证据丢失时先悬停等待 5 秒，恢复失败才以 `insufficient_free_space_evidence` 结束。命令行可覆盖：

```powershell
python run_explore.py --timeout 5400 --return-reserve 120 --max-radius 120 --max-viewpoints 900 --viewpoint-spacing 3 --information-radius 3 --frontier-empty-seconds 10 --completion-frontier-area 1 --minimum-map-gain-area 1
```

这是对可达、可观测区域的尽力探索。`no_new_reachable_frontiers` 表示采样前沿耗尽；时间预算、观察点预算、半径限制（`radius_limit`）、规划搜索上限、路径阻塞分别有独立原因。日志中的 `coverage_guaranteed` 始终为 false，因为没有完整场景边界和真值可证明“整张车库 100% 已探索”。空间不可达、视觉盲区以及其他楼层不算已探索区域。

## 保存内容

```text
data/runs/run_.../exploration/
├── semantic_graph.partial.json   # 探索中的检查点
├── semantic_graph.json           # 返航后的语义结构，无绝对坐标
├── exploration_report.json       # 前沿访问顺序、预算、结束原因、返航确认
├── bundle.json                  # 缓存版本、会话身份与校验和
├── session.json                 # 第二终端的本地连接信息
├── map.html                     # 可刷新地图和目标标记
├── target_result_<任务ID>.json    # 到达确认、返航结果
└── private/
    ├── navigation_cache.npz     # 规划必需的几何、占据地图与坐标基准
    └── navigation_<任务ID>.json # 内部导航轨迹和诊断
```

语义图示例（节选）：

```json
{
  "objects": [
    {"id": "vehicle_02", "class_name": "car", "color": "red", "observed_at": ["place_0007"]},
    {"id": "column_03", "class_name": "column", "color": "unknown", "observed_at": ["place_0007"]}
  ],
  "relations": [
    {"source": "vehicle_02", "relation": "near", "target": "column_03", "symmetric": true, "distance_m": 2.1},
    {"source": "vehicle_02", "relation": "left_of", "target": "column_03", "basis": "initial_heading", "evidence": "estimated_geometry"},
    {"source": "column_03", "relation": "co_observed", "target": "vehicle_02", "symmetric": true, "frame_count": 12}
  ],
  "places": [{"id": "place_0007", "scan_completed": true}],
  "connections": [{"source": "place_0006", "relation": "traversed_to", "target": "place_0007"}]
}
```

完整记录还包括类别别名、置信度、观测次数和颜色置信度。`observed_at` 指在哪个探索点看见物体，并不意味着物体位于无人机观察点。共视关系只累计同一同步帧内真正重新观测的对象。

左右、前后以首次起飞前的机头方向为固定参考，不随之后的转向改变；上下使用重力方向。近邻关系默认 3 米内，相对方位关系默认 6 米内。这些是根据估计几何生成的关系，不代表已识别“支撑顶梁”“属于某停车位”等功能关系。

`semantic_graph*.json` 不保存 NED、绝对位置或三维包围盒。二进制缓存和原有飞行 HDF5 仍保存几何/位姿，因为 A*、EGO 和飞控需要它们。探索模式关闭旧的含坐标 `semantic_instances.json` 和逐帧 JSON 输出。

## 目标标注与地图复用

`map.html` 显示已观测自由空间、障碍、探索树连接、语义对象 ID、起点和无人机位置。选中的对象为红色，安全接近点为黄色，规划路径为蓝色；页面每 5 秒刷新，结果保留在文件中。

目标筛选使用本次探索归档中的静态对象，不会因为等待了几分钟就按 `max_age` 丢弃地图记忆。到达确认仍要求实时感知的新观测。找不到目标或没有可达观察点时不会起飞。

两个阶段共享同一控制会话，归档通过会话 ID 和校验和检查。重启控制进程、UE/PX4、改变场景后应重新探索；本版不提供跨会话重定位。会话监测可见的飞控计时倒退、遥测停滞和位置跳变，但不能证明所有隐蔽的坐标系变化都能被检测到。

探索因预算结束但成功返航时，可以导航到已经记录的对象；因感知、飞控故障而中止且未确认返航时，不进入待命导航状态。

## 验证

离线测试覆盖前沿收益排序、连续访问、仅在停滞时恢复扫描、预算退出、不可达候选、JSON 无绝对坐标、相对方位、共视去重、缓存校验、会话隔离、重复请求拒绝、落地后状态复位、从保存对象导航并复核、返航及 HTML 目标标注。

```powershell
python -m pytest -q tests/test_dfs_workflow.py
```

实际 UE/PX4 飞行和车库覆盖率仍需场景联调确认；离线模拟载具测试不能代替飞行验收。
